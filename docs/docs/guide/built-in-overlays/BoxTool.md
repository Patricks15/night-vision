# BoxTool

A tool for measuring percentages & time ranges. Just add this to your `overlays` array:

```js
{
    name: 'BoxTool',
    type: 'BoxTool',
    data: [],
    props: {},
    settings: {
        zIndex: 1000
    }
}
```